package com.example.anirudh.Validator;

public class GetAllEmployeesValidator implements Validate {
    /**
     * GetAllEmployees Validator. GetAll in itself does not need specific validation
     * @return
     */
    @Override
    public boolean validate() {
        return true;
    }
}
